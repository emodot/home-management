# Public values only — everything here is shipped to the browser.
# Local dev: run `pnpm db:start` and copy the API URL + anon key it prints.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=
VITE_APP_URL=http://localhost:5173
